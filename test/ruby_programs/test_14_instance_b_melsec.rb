# 並行実行の確認 B (test_13_instance_a.rb と対で使う)
#
# 対象機種: Q, iQ-R
#
# **キーエンス版から機種名だけを置き換えたものです** (`DM` → `D`、`MR` → `M`)。
# アドレスはそのままで、faRuby の D4000-D5999 とも GX Works2 が一時変数に使う
# D6144 以降とも重なりません。
#
# A とはレジスタの使い方も演算も違うプログラムにしてある。
# 両方が同じ Z レジスタを使い回すため、片方の途中経過が
# もう片方に混ざれば結果が狂う。
#
# 期待値: $D410 = 2000
i = 0
n = 0
while i < 1000
  n = n + 2
  i = i + 1
end
$D410L = n
