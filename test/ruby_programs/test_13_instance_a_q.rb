# 並行実行の確認 A (test_14_instance_b.rb と対で使う)
#
# 対象機種: Q
#
# **キーエンス版から機種名だけを置き換えたものです** (`DM` → `D`、`MR` → `M`)。
# アドレスはそのままで、faRuby の D4000-D5999 とも GX Works2 が一時変数に使う
# D6144 以降とも重なりません。
#
# 2つのインスタンスに別のプログラムを載せ、同時に走らせて
# 互いの結果を壊さないことを確かめる。
#
# A は DM400 を 1 ずつ、B は DM410 を 2 ずつ数える。
# 期待値: $D400 = 1000, $D410 = 2000
#
# 実機でのみ意味を持つ検証 (シミュレータはインスタンスを持たない)。
i = 0
while i < 1000
  i = i + 1
end
$D400L = i
