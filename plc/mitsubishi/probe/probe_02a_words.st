(* faRuby probe 2a: 32 ビットの計算。説明は README.md
 * 11 は候補を並べてある。通らない行を消して、通ったものを教えてください *)
DMOV(TRUE, K100000, D190);
DMOV(TRUE, K200000, D194);

(* 11b  D+ は構文エラーだった。綴りに直した形 *)
DPLUS(TRUE, D190, D194, D200);

(* 11c  素の式。**通るが 16 ビットなので答えが違うはず** (34464 + 3392)
 * 罠を見えるようにするため、別のデバイスへ出す *)
D206 := D190 + D194;

(* 12 *)
Z0 := 14;
DMOV(TRUE, D190Z0, D204);

(* 13 *)
D210 := 0;
IF D190 < D194 THEN
  D210 := 1;
END_IF;

(* 14 -2.2 *)
EMOV(TRUE, E-2.2, D260);
INT(TRUE, D260, D212);

(* 15 2.7 *)
EMOV(TRUE, E2.7, D262);
INT(TRUE, D262, D214);
