# KV スクリプトは改行コードを LF に固定する
#
# core.autocrlf = true の環境では、チェックアウト時に LF が CRLF へ変換される。
# vm_core.kvs は tools/kvs_generator.rb が LF で生成するため、変換されると
# test_kvs_generator.rb のバイト一致検証 (手編集の検出) が誤って失敗する。
*.kvs text eol=lf
